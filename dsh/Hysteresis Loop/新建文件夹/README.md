# Hysteresis Loop Lab Report

This workspace contains a minimal LaTeX setup for a physics lab report.

## Build (Windows)

Compile with XeLaTeX (recommended for Chinese text):

1. Open a terminal in the workspace.
2. Run:
   
   xelatex main.tex

If you use TeX Live or MiKTeX, ensure XeLaTeX is installed.
